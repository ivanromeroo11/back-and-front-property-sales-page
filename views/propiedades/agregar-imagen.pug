extends ../layout/admin

block styles
   
block contenido 
    
    .py-10 
        h1.text-4xl.my-10.font-extrabold.text-center Bienes 
          span.font-normal Raices 
        h2.text-center.text-2xl.font-extrabold= pagina

        
        div(class="bg-white shadow py-8 px-4 rounded mx-auto max-w-4xl my-10 md:px-10")

            form.space-y-8(method="POST" action="/propiedades/crear")
                input(type="hidden" name="_csrf" value=csrfToken) 
              
                input(class="w-full py-3 bg-green-600 hover:bg-green-500 rounded-md text-white font-bold uppercase cursor-pointer" type="submit" value="Publicar Propiedad")

block scripts
   